# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
# First-time setup
bundle install
npm install

# Development (Jekyll + Tailwind in watch mode)
npm run dev

# Or separately in two terminals:
bundle exec jekyll serve --livereload
npm run css:watch

# Production build
npm run build
```

`assets/css/main.css` is generated by Tailwind and gitignored — it must be built before Jekyll can serve the site.

## Architecture

Jekyll 4 static site · Tailwind CSS 4 · Alpine.js 3 (CDN) · German only

For a C4 Context+Container diagram and more detail on deploy/CI, see [`docs/architecture.md`](docs/architecture.md). For the ongoing agent-automation rollout (Jira integration, PR-gated agent loop, etc.), see [`docs/automation-plan.md`](docs/automation-plan.md).

### Deploy

Two deploy pipelines run on every push to `main`:
- **Azure Static Web Apps** (`.github/workflows/azure-static-web-apps-brave-smoke-037a8b003.yml`) — canonical deploy target (decided 2026-07-23).
- **GitHub Pages** — legacy, runs in parallel. It cannot be disabled via API/settings because this repo is named `carofuerhunde.github.io` (org/user Pages repo); GitHub refuses deactivation (`422`). Treat Azure as canonical in docs only — both hosts stay live technically.

### Branch naming / Jira convention

Branches follow Jira ticket keys, e.g. `CARO-11`, `HUN-5` — two Jira projects (`CARO`, `HUN`). Convention: ticket → branch (`<KEY>-<number>`) → PR referencing the ticket. This is the intended hook for the future agent automation described in `docs/automation-plan.md` (Milestone M2+): an agent will read a Jira ticket, implement on a matching branch, and open a PR linking back to it.

### Design tokens
Defined in `assets/css/input.css` via `@theme`:
- `--color-primary: #FF8800` → use as `text-primary`, `bg-primary`, `bg-primary/10`, `hover:bg-primary-dark`, etc.
- `--color-primary-dark: #E67A00`

### Content data (`_data/`)
All content that repeats across pages lives here:
- `navigation.yml` — main nav links (header + footer)
- `services.yml` — 6 service cards with `slug`, `icon`, `short`, `details` (details rendered via `| markdownify`)
- `team.yml` — team member profiles
- `welpen.yml` — puppy directory (sorted A–Z in `welpen.html`)

### Layouts & includes
- `_layouts/default.html` — base layout; loads Inter font, Tailwind CSS, Alpine.js CDN, jekyll-seo-tag
- `_layouts/page.html` — simple content page with page-hero header section (extends default)
- `_includes/header.html` — sticky header with Alpine.js mobile menu (`x-data="{ open: false }"`)
- `_includes/footer.html` — dark footer with service list from `_data/services.yml`

### Pages
| File | URL | Notes |
|------|-----|-------|
| `index.html` | `/` | Hero, services grid, team teaser, CTA |
| `leistungen.html` | `/leistungen/` | Loops `_data/services.yml`; anchor links via `id="{{ service.slug }}"` |
| `team.html` | `/team/` | Loops `_data/team.yml` |
| `preise.html` | `/preise/` | Pricing — fill in manually |
| `termine.html` | `/termine/` | Workshops + etermin.net booking |
| `welpen.html` | `/welpen/` | Loops `_data/welpen.yml` sorted A–Z |
| `kontakt.html` | `/kontakt/` | Formspree contact form + call-to-action |
| `impressum.md` | `/impressum/` | Legal — uses `page` layout |
| `datenschutz.md` | `/datenschutz/` | GDPR — uses `page` layout |

### Site-wide config (`_config.yml`)
`site.phone` and `site.booking_url` are used in header, footer, and multiple pages. Change them once here.

## Before launch checklist
- [ ] Replace Formspree `PLACEHOLDER` in `kontakt.html`
- [ ] Fill in real operator data in `impressum.md` and `datenschutz.md`
- [ ] Add team photos to `assets/images/` and update `_data/team.yml`
- [ ] Add service details to `_data/services.yml` (`details` field, supports Markdown)
- [ ] Add pricing to `preise.html`
- [ ] Verify `booking_url` in `_config.yml`
- [ ] Add a map embed to `kontakt.html`
